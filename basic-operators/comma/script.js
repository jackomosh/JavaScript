'use script'

let a = (1 + 2, 3 + 4)
alert(a) // 7, since the result is of the last expression only 3+4

// the comma throws everything away except the last expression

// remeber that prompt returns user input as a string