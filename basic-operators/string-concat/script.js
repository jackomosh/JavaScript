'use strict'

let s = "My " + "Name" // concatinates 2 strings
alert(s);

alert(1 + "2"); // Note that if one operand is a string the other one is converted to a string
alert('2' + 2); // here also

// Only + operator works that way

alert(6 - '2'); // 4; in subtraction 2 is converted to a number
alert('6' / '3'); // 2; in division 2 is converted to a number

